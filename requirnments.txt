# Core
Django==5.2.8
djangorestframework>=3.14

# PDF extraction + OCR stack
pymupdf>=1.23.0           # fast PDF text & render
pdfminer.six>=20221105    # fallback extractor
tika>=1.24                # optional, requires Java on the host (tika-python)
Pillow>=9.5.0             # image handling for OCR
pytesseract>=0.3.10       # Tesseract python wrapper (requires tesseract binary installed)

# Skill matching / fuzzy
rapidfuzz>=3.0.0

# NLP (optional, for better entity/title detection)
spacy>=3.6.0
# Note: install the spaCy model separately (see below)

# DB / background (project likely uses these)
mysqlclient>=2.1.1
celery>=5.3.0
redis>=4.5.0

# Utility
python-dotenv>=1.0.0

# Optional: small convenient installer for the spaCy model
# It's recommended to install the model with: python -m spacy download en_core_web_sm
# Alternatively add model wheel URL for a specific version, e.g.:
# en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.6.0/en_core_web_sm-3.6.0-py3-none-any.whl